# Installing Superrefactoring in OpenCode

From this repository:

```sh
opencode plugin add .
```

The plugin registers the local `skills/` directory and injects a small reminder about when to use the refactoring skills.
