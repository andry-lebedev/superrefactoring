# OpenCode Notes

Superrefactoring follows the same broad OpenCode shape as Superpowers:

- `.opencode/plugins/superrefactoring.js` registers the local skills path.
- `.opencode/INSTALL.md` contains the local install command.
- The plugin injects a small reminder about the two refactoring skills.

Keep OpenCode bootstrap text short. The full skill bodies should remain in `skills/`.
